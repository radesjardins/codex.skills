# 2026-06-06 — Doc update prompt

Generated by rad-plan. Paste into Codex to reconcile durable docs
with decisions made during this planning session. Delete once the updates land.

---

During planning on 2026-06-06 we made decisions that affect durable docs the planner
does not own. Please:

1. **docs/prd.md** — Record that the URL shortener is **single-tenant only for v1**, and
   that custom/vanity codes are an explicit non-goal for v1. Mark any earlier wording
   implying multi-tenant or vanity-code support as superseded.

2. **docs/reference/decision-log.md** — Add an active decision: "Idempotent shortening
   via `ON CONFLICT (url_hash)` with an atomic `UPDATE ... RETURNING` click counter
   (2026-06-06)." A detailed ADR is not needed — the rationale fits one line.

3. **docs/architecture.md** — Note that the click counter relies on a single-statement
   atomic update, so any future read-replica or caching layer must not serve stale
   counts on the redirect path.

After updating, confirm each file changed and delete this prompt.
